/**
 * @deprecated Import from './build-context' instead.
 * This re-exports `createContextBuilder` and related types
 * for backward compatibility during the transition.
 */
export { createContextBuilder, type ContextBuilder, type ContextBuilderDeps, type RequestMessageBuildOptions } from './build-context';
